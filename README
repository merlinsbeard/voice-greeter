==========
Creates a mp3 file that contains Greetings, Weather, and News
============

Installation
1. Create Virtualenv

 `$ virtualenv -p /usr/bin/python3 venv`

2. Install requirements

 `$ pip install -r requirements.txt`

3. Run Script

  `$ python greetings.py <name> <area>`

4. Output Sound File is inside the Sound Folder
